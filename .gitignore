node_modules/
test-results/
playwright-report/
.history.json
.claude/
npm-debug.log*
.DS_Store
*.log

# Wrangler local dev cache/state (Pages/Workers).
.wrangler/
# Output of `wrangler pages functions build` — a verification-only bundle.
_worker.bundle

# cf-spike/ — the throwaway proof-of-concept that de-risked running the AMP
# validator wasm on the Workers runtime. The production version now lives in
# functions/_lib/validator*.js; the spike is kept locally for reference only.
cf-spike/

# Local provider API keys — never committed.
.env

# Reference-corpus source emails are never committed to any public artifact
# (see corpus/README.md — "never redistributed" is a non-negotiable project rule).
# Only the distilled, brand-agnostic patterns/ outputs persist into the repo.
corpus/
.data/
